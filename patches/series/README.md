# Veil Patch Queue

The patch inventory in `patches/patch-inventory.json` is the source of truth for Veil patch IDs, status, touched upstream files, and verification hooks.

Current authored patches are intended to apply in inventory order.
